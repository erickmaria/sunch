import { Builder } from 'selenium-webdriver'

export default async function GetAnswer(): Promise<any> {

  // let driver = await new Builder().forBrowser('chrome').build();


  
  // return driver.quit()
}
